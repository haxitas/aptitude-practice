# PHASE_NOTES

## Phase1 テスト3(2026-09-22)

### 完了
- `js/logic/t3.js`(純粋関数): 図形問題(直前と同じ並び・同じ向きは出さない)、計算問題と4択、音声の5語(重複は1組・隣り合わない)、音声タスクの進行(`stepAudio`)、声の選び方(`pickVoice`)、採点と記録。
- `js/tests/t3.js`: 3タスクを1つの rAF ループで回す。図形と計算は答えたらすぐ次へ。音声は `stepAudio` で時刻に沿って進める。押したボタンは最低 `answerFeedbackMs`(300ms)薄くする(正誤は出さない)。
- `js/core/landscape.js`(SPEC 外、承認済み): 縦のときは全面に「端末を横にしてください」を出し、開始ボタンを無効にする。T6 でも使う。
- 設定の型の確認に、配列(数値だけ)を追加した(`calcDistractorOffsets`)。
- 履歴の表は、内訳のうち `inHistory: false` のもの(回答数・正答率)を出さない。
- 単体テスト: 全体で 76 件(t3 は 25 件、設定の型の確認を storage に2件追加)。わざと壊すと落ちることを3種類で確認した(計算の途中の値が負・傾き±15°・重複2組)。
- ローカル(1180×820)で240秒を1回通して記録が保存され、履歴の表に出ることを確認した。

### 未解決
- 実機での確認(ユーザー待ち): 音が出るか、英語の声か、間隔、途切れ、マナーモード、縦の表示。
- この PC の Edge には英語の声がなく(日本語の声だけ)、声を指定しない(④)動作になった。1語の読み上げに約1.2〜1.7秒かかるため、「読み終えるまで待つ」(T3-A)により語の間隔は約1.4〜1.9秒に延びた。英語の声がある端末での間隔は実機で確かめる。
- まだ push していない(レビュー待ち)。

### 仮定(承認済み。計画の T3-A〜L)
- T3-A. 語の間隔は、組の開始時刻を基準に読み始めを 1000ms ごとにする。前の語を読み終えていなければ待つ(読み上げは切らない)。
- T3-B. 重複する2語は隣り合わない位置に置く。
- T3-C. 重複を入れるかは組ごとに独立に抽選する(確率 50%)。
- T3-D. 読み終えて5秒答えなければ未回答(回答数に入れない)。次の組は回答と同じく 1000ms 後。
- T3-E. 終了時刻に残っていた問題・読み上げ中や回答待ちの組は数えない。
- T3-F. 正誤は出さない。答えたタスクはすぐ次の問題に変わる。
- T3-G. プレイ中に縦になったら中断し、保存しない。
- T3-H. 非表示・描画が止まった(フレームの間隔が `stallAbortMs`=1000ms を超えた)ときは中断し、保存しない。
- T3-I. 計算の誤答は正解 ±1・±2・±10 のうち、0以上で重ならないものから3つ。正解が0になってもよい。
- T3-J. 三角形の傾きは −10°〜+10° の一様乱数。傾けるのは三角形だけ。
- T3-K. 記録の内訳は各タスクの正答数・回答数・正答率(整数%、回答0なら null)と音声の未回答数。履歴の表は正答数と未回答数。
- T3-L. キーボード操作は付けない。
- 声の選び方: ① en-US で default の声 → ② en-US(おもしろ系を除き、localService を優先)→ ③ ほかの en-* の声 → ④ 声を指定せず lang='en-US' だけ。使う声は開始画面とプレイ中の上部にいつも出す。
- 開始ボタンの click の処理の中で、`speechSynthesis.cancel()` のあと、同期的に1語目の `speak()` まで進める(iOS 対策)。
- 読み終わりは `onend`。来ない場合は、読み始めから `speechEndFallbackMs`(2500ms)たつか、`speaking` と `pending` が false で読み始めから `speechIdleGraceMs`(250ms)たったら読み終わりとみなす。`onerror` も読み終わりとみなす。
- 計算の引き算は、選べるとき(途中の値が1以上)に確率 `calcSubtractRate`(0.5)で選ぶ。

### 既知の限界
- 計算の4択は、選択肢どうしの差から正解を割り出せることがある(誤答が正解 ±1・±2・±10 から作られるため)。SPEC の指定どおりなので直さない。
- 声の一覧が空の端末(iOS で起こることがある)では、声を指定せず lang だけで読む。その場合、どの声で読まれるかは端末しだい。
- 読み上げの速さや、1語にかかる時間は声によって違う。遅い声では語の間隔が 1000ms より長くなる(T3-A)。

### 次の初手
- レビュー後に push し、Pages のビルドを確かめる。
- ユーザーの実機(iPad・iPhone)で音声まわりを確かめてもらう。
- その後、Phase2(テスト6)に入る。

## Phase0(2026-09-22)

### 完了
- 土台: ハッシュルーティング(`#/` `#/t1`〜`#/t6` `#/history` `#/settings`)、メニュー(各テストの前回/最高/回数)、画面を切り替えるときの後片付け。
- `js/core/`: rng(mulberry32)、storage(apt_results は読み込みと追加だけ、apt_settings は読み込みだけ)、settings(t1〜t6 の既定値と保存値の合成)、stats(集計)、timer(rAF のループ)、result(共通の結果画面)、catalog(テスト一覧)。
- テスト2: `js/logic/t2.js`(生成・判定・採点。純粋関数)と `js/tests/t2.js`(描画と入力。図形は SVG)。
- 簡易履歴: テストを選び、直近20回を新しい順に表で出す。最高点(全記録)、直近5回の平均(小数1桁)、回数(全記録)も出す。
- 単体テスト: `node --test` で 49 件。わざと壊すと落ちることを3種類で確認した(誤押しの減点・連続上限・追加処理)。
- ローカル(1180×820)で、既定値の180秒を1回通して記録が保存され、履歴の表に出ることを確認した。
- 結果画面では「もう一度」にフォーカスを当てず、フォーカスを外す(終了直後のスペースキーで結果画面が消えないようにするため)。
- Phase0 の完了条件: ユーザーが PC と iPad の実機で、公開 URL のテスト2を最後まで遊べて記録が履歴に出ること、PC のスペースキーで押せることを確認した(2026-09-22)。

### テスト2の修正(2026-09-22、ユーザーの決定)
- 制限時間を 180秒 → 120秒 にした(`DEFAULTS.t2.durationSec = 120`。表示120回・一致30回)。SPEC §6 も変更。
- 左右が同じ表示を2回続けて出さないようにした(`maxConsecutiveMatches = 1`。k=1 で置ける一致の上限は floor(121/2)=60回)。生成の仕組みは変えていない。SPEC §6 も変更。
- 押したことが必ず見えるようにした。理由: これまでは「次の表示まで薄くする」だけだったため、表示の終わり近くで押すと薄い時間が短く、ほとんど見えなかった(計測: 表示の 902ms で押すと 131ms、965ms で押すと 83ms)。受け付けた押下のあとは、その表示が終わるまで、かつ押してから `pressFeedbackMs`(既定 300ms)たつまで薄くする。時間の管理は rAF のループの中で行う。正誤は出さない。薄いあいだでも新しい表示なら押下を受け付け、判定は押した時点の表示に対して行う。
- これまでの記録はそのまま残す(各記録に設定値が入っているので見分けられる)。

### 未解決
- テスト2の修正(120秒・連続なし・押下の表示)は、まだ push していない(レビュー待ち)。
- ブラウザ操作ツールのキー送信は `key:"" code:""` のイベントになるため、ツールでのスペースキーの確認は `code:'Space'` の合成イベントで行う(実際のキーボードではユーザーが確認済み)。
- 設定画面・グラフ・既定値と違う回の印・書き出し/読み込み・全消去(Phase4)。
- T6 の初速・加速度・速度の上限・障害物の間隔は、SPEC に数値がないため既定値に入れていない(Phase2 で決める)。

### 仮定(承認済みの解釈と決定)
- A. 一致は連続させない(`maxConsecutiveMatches=1`)。2026-09-22 にユーザーの判断で「3回以上続かない(既定 2)」から変更した。
- B. 一致の回数は N×確率 に固定し、構成的に並べる(不一致 N−m 個の隙間 N−m+1 か所に、各 k 個までの容量で一致を1つずつランダムに配る)。棄却サンプリングは使わない。
- C. 直前とまったく同じ表示は出さない(候補から外して選ぶ)。
- D. 押したときに正誤は出さない。ボタンを薄くするだけ(その表示が終わるまで、かつ押してから最低 `pressFeedbackMs`)。
- E. プレイ中にページが非表示になったら中断し、保存しない。さらに、描画が止まって表示が1つ以上飛んだとき(i > cur + 1)と、最後の表示を描く前に終了時刻を過ぎたときも、中断して保存しない。メニューに「描画が止まったため中断しました(記録は保存していません)」と出す。
- F. 同じ id の記録は追加しない。
- 1. 履歴に「直近5回の平均」を出す(5回未満ならある分だけ、小数1桁)。表は直近20回を新しい順。最高点と回数は全記録から。集計は `js/core/stats.js`。新旧は配列の順ではなく `date` で決める。
- 2. Phase0 では削除の関数を作らない。Phase4 で全消去の関数を1つだけ例外として追加する。
- 3. 保存に失敗したとき(apt_results が壊れている・容量超過など)は、点数と内訳を出したうえで「保存できませんでした(理由)」と表示する。再試行はしない。
- 4. apt_settings が JSON として読めないときは既定値で動かし、メニュー上部に注意を出す。apt_settings は書き換えない。型が合わないキーだけ既定値に戻す(NaN・Infinity も不可)。
- 5. 的中0回のときの平均反応時間は、保存値 null、表示「—」。値があるときは整数ミリ秒。
- 6. 一致の回数 m = Math.round(N × 一致確率)。
- 7. 必要な不一致の数は ceil(m/k)−1。置ける一致の最大数は floor(k(N+1)/(k+1))(N=180, k=2 なら 120)。超えたらエラーにし、使える確率の上限(例: 0.667)をエラー文に入れる。
- 8. 終了は N×間隔の時点。残り時間も N×間隔から数える。
- 9. 点数 = Math.round(Math.max(0, hits*100/(hits+misses) − fa*penalty))。hits+misses=0 なら 0。
- 表示の件数(直近20回・直近5回)は `settings.js` の `DISPLAY` に置いた(利用者が変える値ではないので apt_settings には入れない)。

### 既知の限界
- 反応時間の起点は、その表示を描いたフレームの rAF 時刻。実際に画面に出るのはその後なので、反応時間は最大で約16ms(1フレーム)長めに出る。
- 表示が切り替わった直後に処理された押下は、前の表示のあいだに押したものでも次の表示のものとして数えられ、反応時間は 0ms に切り上げられる(`Math.max(0, ts - frameTs)`)。起きる時間の幅は1ms程度でまれなので、直さない。
- 「描画が止まったため中断」の処理(E の拡張)は、狙って確かめる方法がない。ただし 2026-09-22 に、ブラウザ操作ツールの画面が非表示になって描画が止まったとき、実際にこの中断が働いた(メニューに「描画が止まったため中断しました(記録は保存していません)」が出て、記録は増えなかった)。

### 次の初手
- テスト2の修正をレビュー後に push する。
- Phase1(テスト3)の計画をレビュー・承認してから実装に入る。
- 公開済み(2026-09-22): リポジトリ https://github.com/haxitas/aptitude-practice (公開、main の / から Pages)、URL https://haxitas.github.io/aptitude-practice/ 。公開 URL の index・js・css が 200 で返り、PC のブラウザでメニューに6テストが出て、コンソールにエラーがないことを確認した。
