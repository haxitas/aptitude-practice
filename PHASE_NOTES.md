# PHASE_NOTES

- 2026-09-22 から実装役は Codex。

## 2026-09-23 実機報告後の修正: 時間表記・テスト4

### 完了
- 共通の `formatDuration` で正の整数秒を分・秒に直し、6テストの開始説明へ適用した。残り時間の `m:ss` 表示やタイマーの長さは変えない。
- テスト4の例題は機首NE・相対90°、塔SE、自機NW・向きNEへ変更した。説明はADF→塔の方位→自機の位置の3段階。計器と解答欄の見出しを具体化し、ADFの右90°・後ろ180°・左270°を針と重ならない位置に描いた。
- 結果と履歴の内訳は回答数・正答数・位置だけ正解・向きだけ正解。新規記録のdetailにはcorrectを保存する。旧記録にキーがない場合は「—」を表示し、既存のapt_resultsは書き換えない。
- `showPreviousAnswer` の既定値はtrue。設定画面で表示/非表示を選べる。本番の初問では非表示、回答後に次問の下へ前問の正解と自分の答えの○/×を出す。falseなら出さない。
- テスト先行で `formatDuration`・`previousT4Feedback` の未exportと設定項目不足を確認。故障注入で「4分0秒」を返すと時間表記テストが失敗し、例題の機首をNへ戻すと斜め例題テストが失敗した。どちらも復元した。
- Codex内ブラウザの実操作: 変更前は60秒設定、変更後は20秒設定でSVGのrotate角度から5問を計算した。変更後、正しいマスと向きを押すと5点/正答数5/回答数5。塔の側のマスと正しい向きを5問押すと0点/正答数0/回答数5/位置だけ正解0/向きだけ正解5。計器角度と `solutionFor` の出力は両回の全問で一致した。コンソールerrorは0件。
- 6テストの開始画面で既定値の3分・2分・4分・3分・3分・3分を確認。390×844でも斜め例題、ADF目盛り、回答欄を確認した。テスト4の診断用設定は既定値へ戻した。
- `node --test` は全191件成功。

### 未解決・次の初手
- この修正はローカルcommitのみ。レビュー後にpushする。
- 実機でADF目盛りと3段階の例題が読みやすいか確認する。

## Phase4 設定画面・成績管理(2026-09-23)

### 完了
- `js/logic/settings-form.js`に、画面へ出す設定項目のラベル・単位・範囲・整数条件と、入力の変換・検証を純粋関数で実装した。内部の描画停止、Canvas余白、再試行上限、回答表示時間などは画面へ出さない。
- 空欄・非数・範囲外・整数欄の小数に加え、最小値と最大値、T2の一致数と連続上限、T3の2桁個数と誤答差、T5の点の直径と最小間隔、T6の速度と小穴の個数・重なり・範囲を検証する。弾いた場合は項目ごとの理由を表示し、保存しない。
- 設定画面はテストごとの折り畳み、保存、テスト単位の「既定値に戻す」を持つ。保存値は次に画面を開いて開始するテストから反映し、プレイ開始時の設定スナップショットが従来どおり記録へ入る。
- `saveSettings`は`apt_settings`だけを書き、成功時・例外時とも`apt_results`を1文字も変えないことを単体テストした。失敗時は画面用の理由を返す。
- `js/logic/records-transfer.js`に、正式形式`format/version/exportedAt/records`の作成、過去の`{records}`形式を含む全件検証、既存順序を保つ重複除外を純粋関数で実装した。途中に不正記録があれば全体を拒否する。
- JSON書き出しはBlobと`download`を使い、常に画面表示・コピーの代替経路も置いた。読み込みは既存idとファイル内重複を無視し、「追加n件、重複のため無視n件」を表示する。
- 読み込みの保存は、全件検証後に`apt_results`へ1回だけ書く。書き込み例外では元の文字列を保ち、部分適用せず、理由を返すことを単体テストした。
- `storage.js`にappend-onlyの唯一の例外として`clearResults`を追加し、理由のコメントを置いた。設定画面の1回目のボタンで警告と最終ボタンを表示し、2回目だけ消去する。設定は消さない。
- テスト先行では`settings-form.js`・`records-transfer.js`が存在せず、storageの新しいexportもなく失敗することを確認した。故障注入③T2の配置可能性検証を外す、④先頭記録だけ検証して末尾を見ない、⑤重複除外を外す、⑥別の削除関数を公開する、の4件で対応テストが落ちることを確認して戻した。
- Phase4完了時の単体テストは全171件。
- Codex内ブラウザで、T2の一致確率0.9を「一致は最大60回」と拒否すること、0.5の保存と既定値戻し、T4を10秒へ変更して次の回を完走し、記録JSONへ`durationSec: 10`が入ることを確認した。
- JSON読み込みは、正しいファイルで追加1件、同じファイルの2回目は重複1件、2件目が不正なファイルは先頭も含め0件追加となり、履歴が42点・1回のままであることを確認した。全消去は第2確認の表示とキャンセルを確認した。コンソールerrorは0件。

### 未解決
- Codex内ブラウザではBlobダウンロードの開始表示は出たが、ブラウザ操作APIでダウンロード完了イベントを取得できなかった。画面表示とクリップボードへのコピーは実物のJSONで確認した。iPad Safariのファイル保存は実機確認に回す。
- ユーザー実機で、設定欄の入力、JSON保存・ファイル選択からの読み込み、2段階の全消去、明るい/暗い履歴グラフを確認する。
- まだpushしていない(レビュー待ち)。

### 仮定(承認済み。計画のP4-A・F〜L)
- 画面には練習の時間・難易度・問題生成・T6物理値を出し、正常動作を守る内部値は出さない。
- 読み込みは1件でも不正なら全体を拒否し、同じidは既存記録を優先する。既存配列を保ち、新規分をファイル順で末尾へ追加する。
- 書き出しは版付きの正式形式にし、読み込みは従来の`{records:[...]}`も受け付ける。
- 「既定値に戻す」と全消去は対象テスト・成績だけに限定し、他テストの設定は残す。全消去はブラウザ標準ダイアログに依存しない2段階の画面操作にする。

### 既知の限界
- ファイル保存の最終的な動作はSafari/iPadOS側にも依存するため、画面表示・コピーを常設している。

### 次の初手
- Phase4の2 commitをレビューし、承認後にpushしてPagesと実機を確認する。

## Phase4 履歴グラフ(2026-09-23)

### 完了
- `js/logic/history.js`に、0起点の点数目盛り、Canvas座標、直近20件の履歴モデル、記録時設定と現在の既定値の比較を純粋関数で実装した。表は新しい順、グラフは古い順から左→右に並べる。
- 0件・1件・同点・0点でも有限な座標を返し、1件は横中央へ置く。Y軸は最高点から1・2・5系の区切りを作り、0点だけでも上限を1にする。
- 履歴画面にCanvasの折れ線グラフを追加した。最大5個の日付目盛り、点のタップ/クリックによる日時・点数表示、既定値と違う回の橙色の二重円と表の`※`を表示する。
- CanvasはCSS寸法に`devicePixelRatio`を掛けた内部寸法で描き、`ResizeObserver`で幅の変更に追従する。明るい配色と暗い配色をCSS変数で分けた。
- 記録の設定は現在の`DEFAULTS`の全キーと比較する。既定値側のキーが記録にない場合も差分とし、古いテスト2の180秒記録は現在の120秒と違う回として印が付く。記録だけにある余分なキーは無視する。
- テスト先行では`js/logic/history.js`が存在せず失敗することを確認した。故障注入①0点時のY軸上限を0にする、②記録にない既定値キーを無視する、の両方でテストが落ちることを確認して戻した。
- Codex内ブラウザでは、検証中だけテスト4を1秒にしてアプリ自身に0点の記録を1件保存させ、180秒へ戻したあと履歴を表示した。Y軸0〜1、中央の0点、橙色の設定変更印、表の`※`、点の選択後の`2026/09/23 03:14 / 0点 / 設定変更あり`を確認した。コンソールerrorは0件。

### 未解決
- 設定画面、JSON書き出し/読み込み、成績全消去を次のcommitで実装する。
- ユーザー実機で、iPad横画面の文字・点・凡例、点のタップ、明るい/暗い配色を確認する。
- まだpushしていない(レビュー待ち)。

### 仮定(承認済み。計画のP4-B〜E)
- 設定差分は現在の既定値を基準にする。既定値が後から変わった場合、古い記録に印が付く。
- 表は新しい順、グラフは左が古く右が新しい。Y軸は0固定で、直近20件の点数から上限を決める。
- Canvas上の表示だけに頼らず、既存の表と設定差分の`※`を残す。

### 次の初手
- 設定フォーム・記録の書き出し/読み込み・全消去のテストを先に追加し、現行コードで落ちることを確認する。

## Phase3 テスト1(2026-09-23)

### 完了
- `js/logic/t1.js`に、単位換算・速さ・出会い・追いつき・割合の5種類、重複しない正数の4択、直前と同じ問題を避ける生成、判定・採点・記録を純粋関数で実装した。
- 単位換算はha・a・km²・km・時間を双方向に扱う。速さは距離・時間・速さを求める3形式。出会いは速度和、追いつきは速度差を使い、答えが整数分になるよう距離を構成する。割合は整数になる元の数を構成する。
- SPECの例`L=4.2, a=4, b=3`が36分になること、2000シードで全5種類・正の整数解・4択すべてが重複のない正の整数・桁ずらしは最大1つ、各テンプレート、直前とまったく同じ問題を出さないことを単体テストした。
- 初回実装は非整数の誤答を除外せず、典型誤答が不足すると正解の近傍値で補っており、承認済みT1-Gと違っていた。修正後は加減算の取り違え、時間と分の換算忘れ、100で割らない、換算段階の取り違えなど、問題固有の典型誤答から正の整数だけを残す。`正解×10`・整数になる`正解÷10`は合わせて最大1つだけ選び、典型誤答を含む3つが作れない構成は受け付けない。
- `js/tests/t1.js`で4択を表示し、回答後は正誤を出さず、押したボタンを最低300ms薄くしてから直前と異なる次問へ進む。画面非表示または描画停止時は中断し、終了時の未回答問題は数えない。
- 設定に各生成範囲、既定180秒、回答表示300ms、描画停止1000msを追加し、メニュー・ルーティング・履歴へ登録した。点数は正答数。内訳は回答数と整数の正答率。
- テスト先行では`js/logic/t1.js`が存在せず失敗することを確認した。初回実装の故障注入3件に加え、誤答修正後は①非整数も許す、②桁ずらしを2つ許す、の2種類で2000シードの検査が落ちることを確認して戻した。
- 修正後の単体テストはT1 13件、全体148件。
- Codex内ブラウザで既定の180秒を完走した。結果は46点、回答152、正答率30%。「記録を保存しました」、メニューの「前回46 / 最高46 / 1回」、履歴の同じ記録を確認した。コンソールerrorは0件。
- 誤答修正後、Codex内ブラウザを再読み込みして5種類すべてを表示し、各4択が整数だけであることを確認した。例は、割合`780/160/16/820`、出会い`45/30/180/3分`、速さ`19/14/70/140km/h`、追いつき`2100/7/350/35分`、単位換算`8/80000/8000000/800000m²`。確認は途中終了し、コンソールerrorは0件。

### 未解決
- ユーザー実機で、問題文と4択の読みやすさ、ボタンの押しやすさ、回答後の薄表示と問題切替、180秒の操作感を確認する。
- まだpushしていない(レビュー待ち)。

### 仮定(承認済み。計画のT1-A〜K)
- 5種類は同じ確率で生成し、同じ種類を連続させないことで直前とまったく同じ問題も防ぐ。正誤は表示しない。
- 数値は構成的に選び、出会い・追いつき・割合を含めて正解は正の整数にする。
- 典型誤答から正の整数を3つ作れる構成だけを出し、重複・0・負数・非整数を除く。桁ずらしは1問につき最大1つで、近傍値による穴埋めはしない。
- 画面非表示またはフレーム間隔が1000msを超えた回は中断し、保存しない。

### 既知の限界
- 桁ずらしは各問題に最大1つ含めるため、典型誤答2つと桁ずらし1つの組になる場合がある。

### 次の初手
- Phase3のテスト4・5・1をまとめてレビューし、承認後に3 commitをpushする。

## Phase3 テスト5(2026-09-23)

### 完了
- `js/logic/t5.js`に、直前と異なる3〜13個の問題生成、点配置の入れ替え、1000msごとの切替番号、1問10秒と全体終了の優先順位、採点と記録を純粋関数で実装した。
- 初回実装では承認済み計画から外れ、中心・半径0.3の6点・半径0.68の12点という固定19候補から選んでいた。変更する承認済み理由はなく、実装ミスだったため、通常は円内への棄却サンプリング、上限後だけ格子へ代用する計画どおりの方式へ修正した。
- 通常配置は、1点につき最大`placementAttemptLimit=200`回、配置全体を最大`layoutRestartLimit=20`回やり直す。点の中心は面積に対して一様になるよう半径へ平方根を使い、中心間距離`dotMinDistanceRatio=0.12`以上、点半径0.025を含めて円内に置く。上限後は、最小距離を守る六角格子候補をランダム回転・シャッフルして13点まで必ず返す。
- 1000シードで全点が円内・最小距離以上・13点も必ず配置できることに加え、半径0〜0.3、0.3〜0.6、0.6〜内周限界の全域へ点が出て、旧2重輪以外の点が80%を超えることを単体テストした。同じ値だけを返す乱数で通常配置を失敗させ、格子fallbackになることも確認した。
- `js/tests/t5.js`で円と点をCanvas描画し、問題中は点数を変えずに1000msごとに位置だけを変える。回答後は直前と異なる点数へ進み、問題番号を増やし、押した数字ボタンを最低300ms薄くする。
- 1問10秒で未回答として次へ進む。問題の時間切れとテスト全体の終了が同時なら全体終了を優先し、その問題は未回答に数えない。終了時に表示中の未完了問題も数えない。
- 設定に点の最小間隔0.12、点半径0.025、1点の試行200回、全体の再試行20回、回答表示300ms、描画停止1000msを追加し、メニュー・ルーティング・履歴へ登録した。点数は正答数。内訳は回答数・未回答数・回答した問題の平均絶対誤差(回答0ならnull)。
- テスト先行では`js/logic/t5.js`が存在せず失敗することを確認した。テストのテストは、①内周を縮めて最小間隔を破る、②全体終了と同時でも未回答にする、③誤差を絶対値でなく符号付きにする、の3種類で対象テストが落ちることを確認してから戻した。
- 初回実装の故障注入3件に加え、計画どおりの配置へ直した後も、①円内判定から点半径を外す、②最小距離判定を外す、③全体終了と同時でも未回答にする、の3種類で対象テストが落ちることを確認して戻した。
- 修正後の単体テストはT5 12件、全体148件。
- Codex内ブラウザで既定の180秒を完走した。1000msごとの配置変更、10秒未回答で問題番号が進むこと、回答直後に問題番号が進んで押したボタンが薄くなることを確認した。結果は0点、回答1、未回答17、平均の誤差1。「記録を保存しました」、メニューの「前回0 / 最高0 / 1回」、履歴の同じ記録を確認した。コンソールerrorは0件。
- 配置修正後、Codex内ブラウザで同一問題の位置変更と次問題の計3枚を撮り、点が固定の輪ではなく中心・中間・外側へ不規則に散らばり、円内で重なっていないことを目視確認した。確認は途中終了し、記録は増やしていない。コンソールerrorは0件。

### 未解決
- ユーザー実機で、点の見やすさ、1秒ごとの位置変更で数え続けられるか、11個の数字ボタンの押しやすさ、180秒の操作感を確認する。
- まだpushしていない(レビュー待ち)。

### 仮定(承認済み。計画のT5-A〜I)
- 問題が変わったことは問題番号と押したボタンの薄表示で示し、直前と同じ点数は出さない。正誤は表示しない。
- 平均誤差は回答した問題だけの`|回答−正解|`を平均し、小数1桁に丸める。回答0ならnull。未回答は誤差へ含めない。
- 画面非表示またはフレーム間隔が1000msを超えた回は中断し、保存しない。

### 既知の限界
- 最小距離を満たす棄却サンプリングなので、配置済みの点の周囲は候補から除かれる。すべての有効配置を同じ確率で選ぶ方式ではない。

### 次の初手
- Phase3の次の実装順はテスト1。レビューとpushはテスト4・5・1をまとめた報告後に行う。

## Phase3 テスト4(2026-09-23)

### 完了
- `js/logic/t4.js`に、Nから時計回りの8方位、`塔の方位=(機首+相対角度) mod 360`、塔と反対側の自機位置、64通りから直前と同じ組を除く問題生成、選択状態、位置・向き別の判定、採点と記録を純粋関数で実装した。
- SPECの検証例2件と、8方位×8相対方向の64通りを式と照合する全件テストを入れた。位置・向きの選び直し、片方だけでは決定できないこと、中央の塔を選べないことも単体テストした。
- `js/tests/t4.js`で、コンパスとADF型相対方位計をSVG描画する。3×3マスと8方向ボタンは選択中を枠で示し、両方を選ぶまで「決定」を無効にする。決定後は正誤を出さず、直前と異なる問題へ進み、決定ボタンを最低300ms薄くする。
- 設定に`answerFeedbackMs=300`を追加し、メニュー・ルーティング・履歴へ登録した。点数は位置と向きの両方が正しい回答数。内訳は回答数・位置だけ正解・向きだけ正解。
- テスト先行では`js/logic/t4.js`が存在せず失敗することを確認した。テストのテストは、①自機を塔の反対側にしない、②相対角度を加算せず減算する、③位置だけ正解も得点にする、の3種類で対象テストが落ちることを確認してから戻した。
- 単体テストはT4 11件、全体123件。
- Codex内ブラウザで既定の180秒を完走した。位置だけでは決定が無効、向きも選ぶと有効、決定後に計器が変わり選択が解除されることを確認した。結果は0点、回答1、位置だけ正解0、向きだけ正解0。「記録を保存しました」、メニューの「前回0 / 最高0 / 1回」、履歴の同じ記録を確認した。コンソールerrorは0件。

### 未解決
- ユーザー実機で、SVG計器の針と方位文字の読みやすさ、マス・方位ボタンの押しやすさ、選択枠、180秒の操作感を確認する。
- まだpushしていない(レビュー待ち)。

### 仮定(承認済み。計画のT4-A〜I)
- 相対角度は機首正面を0°として時計回り。自機の位置は塔の方位から180°反対側の8近傍マスとする。
- 中央は塔の表示だけで選択不可。位置と向きは何度でも選び直せ、両方がそろうまで決定を無効にする。
- 決定時に未完了の問題は数えず、終了時に選択途中の問題も数えない。正誤は表示しない。

### 既知の限界
- 計器と解答欄は縦長の画面ではスクロールが必要になる場合がある。ボタンの最小44pxは維持する。

### 次の初手
- Phase3の次の実装順はテスト5。レビューとpushはテスト4・5・1をまとめた報告後に行う。

## Phase2 テスト6(2026-09-22)

### ユーザー実機確認後の変更(2026-09-23)
- 障害物は機体面(`collisionZ`)をまたいだフレームで衝突・通過を判定し、その場で最奥へ戻す。描画対象は`z > collisionZ`だけとし、すれ違い後の拡大表示をなくした。`nearZ`は削除し、投影がz=0以下・非有限を拒否する仕様は残した。
- 位置への追従と指の12%ずらしを削除した。矢印キーと画面内のスティックを常時併用でき、入力ベクトルを足して長さ1に制限し、`位置 += 入力 × moveSpeed × dt`で動かす。既定の`moveSpeed`は1.0断面単位/秒。入力がなければ即停止し、衝突の押し戻し中は入力を無視する。
- スティックは中心からの距離に比例して入力を強くし、円外からの押下は無視する。pointer captureを使い、`pointerup`・`pointercancel`で停止する。矢印キーはプレイ中の既定スクロールを止め、非表示・画面離脱・後片付けで押下状態を消す。
- トンネルとスティックの配置を純粋関数にした。通常は上下、トンネル半径がCanvas高の30%未満になる寸法では左右に置き、両方を上の帯とCanvas内に収める。スティック半径はCanvas高の14%を48〜90pxに制限する。1180×820と844×390で、非重複・画面内・半径44px以上を単体テストした。
- 障害物に、90°の扇形開口と、円周上8候補のうち重複しない3か所に半径0.2の穴を開ける小穴型を追加した。半円・3枚羽根・扇形・小穴は同確率。扇形の辺と中心、小穴の縁は衝突とする。押し戻しは扇形の中央、小穴では最寄りの穴へ向ける。
- 単体テストはT6 34件、全体112件。新しい入力・配置・扇形の315°またぎ・小穴の内外と縁・穴候補の重複なし・多数シードで4種類・判定直後の再配置・描画対象のz範囲を含む。
- テストのテスト: ①扇形を180°にする、②小穴の縁を安全にする、③描画対象のz条件を外す、の3種類で対象テストが落ちることを確認してから戻した。
- Codex内ブラウザ(1180×820)で180秒を1回完走した。Codexのブラウザ操作によるキー送信とドラッグで、矢印キーの移動、スティックの移動、離したあとにつまみが中央へ戻ることを確認した。連続自動操縦スクリプトは使っていない。4種類の障害物を確認し、機体面を過ぎた障害物が画面を覆う拡大はなかった。結果は19点、衝突35、距離197.8 u、最高速度1.6 u/s。「記録を保存しました」と、メニューの「前回19 / 最高22 / 2回」を確認した。コンソールerrorは0件。
- Codex内ブラウザ(844×390)で、トンネルとスティックが重ならず、上部の帯を避けてCanvas内に収まることを確認した。短時間の確認は途中終了し、記録は2回のまま増えなかった。

### 完了
- `js/logic/t6.js`(純粋関数): 断面座標・極座標・角度正規化、キーボードとスティックの入力合成・dt移動、トンネルとスティックの配置、`scale = f / z`の投影、4種類の障害物の生成と衝突判定、基準速度と衝突後の回復、障害物の進行と再配置、採点と記録。
- `js/tests/t6.js`: Canvas 2Dの疑似3D描画、矢印キーとPointer Eventsによるスティック入力、1本のrAFループ、横画面・非表示・描画停止の中断、結果保存。`js/core/landscape.js`と共通タイマー・結果画面を再利用した。
- T6の既定値を`settings.js`へ追加し、`app.js`と`catalog.js`へ登録した。点数は衝突せず通過した障害物数、内訳は衝突・距離(小数1桁)・実際の最高速度(小数2桁)。
- トンネルとスティックはCanvasの寸法から配置し、1180×820と844×390を単体テストした。
- 機体面以下の障害物は描かず、その場で最奥へ戻す。投影はzが0以下・非有限ならエラーにする。
- 速度は`baseSpeed(t)=min(1.6, 0.8 + 0.02t)`。衝突で現在速度を50%にし、`recoveryAcceleration=0.16`で基準速度へ戻す。最高速での衝突は約5秒で回復する。
- 初回実装時の単体テストはT6 28件、全体105件。2026-09-23の変更後は上記のT6 34件、全体112件。
- テストのテスト: ①開口端を`<`から`<=`に変更、②角度正規化を削除、③衝突判定へ回転角を渡さない、の3種類で対象テストが落ちることを確認してから戻した。
- Codex内ブラウザ(1180×820)で180秒を1回完走した。座標クリックでマウスを右下・左上へ動かした。結果は22点、衝突31、距離206.3 u、最高速度1.6 u/s。「記録を保存しました」、メニューの「前回22 / 最高22 / 1回」、履歴の同じ1行を確認し、コンソールerrorは0件だった。
- Codex内ブラウザ(844×390)でも開始画面・topbar・Canvasが収まることを確認し、短時間の確認は途中終了した。記録は1回のまま増えなかった。

### 未解決
- ユーザーのiPad実機確認: スティックの操作感と即停止、3分間の滑らかさ、4種類の見分けやすさ、すれ違い時に画面が覆われないこと、開口端の衝突感、押し戻し、縦回転時の中断、Safariのアドレスバーとセーフエリア、結果・履歴の保存。
- Codex内ブラウザの読み取り分離環境では`localStorage`を直接取得できなかった。アプリ自身の保存成功表示、メニュー集計、履歴の行で保存を確認した。
- 2026-09-23の変更は`5eadf84`で公開済み。

### 仮定(承認済み。計画の T6-A〜L と追加変更)
- T6-A. 点数は衝突せず開口を通過した障害物だけを数える。衝突した障害物は得点に入れない。
- T6-B. 当たり判定は機体中心の点で行い、見た目の翼幅は入れない。
- T6-C. 開口角度の端、中心小円の縁、半円の直径上は衝突とする。
- T6-D. 半円・3枚羽根・扇形・小穴は同確率。半円4方向、羽根2回転方向、扇形4方向はそれぞれ等確率で、連続制限は付けない。
- T6-E. 衝突がなければ`baseSpeed(t)`に沿う。衝突で現在速度を50%にし、回復加速度0.16で約5秒かけて`baseSpeed(t)`へ追いつき、超えない。
- T6-F. 矢印キーとスティックを合成して長さ1に制限し、`moveSpeed=1.0`とdtで移動する。入力がなければ即停止する。
- T6-G. スティックは通常トンネルの下に置き、縦配置でトンネル半径がCanvas高の30%未満になる寸法では右下へ置く。半径はCanvas高の14%を48〜90pxに制限する。
- T6-H. 衝突時は機体の実位置を350msかけて安全方向へ0.28動かし、その間は通常入力を無視する。
- T6-I. 羽根はテスト全体の経過時間に比例して速くする。初速30°/s、角加速度0.15°/s²。
- T6-J. 距離は小数1桁、実際の最高速度は小数2桁で保存する。内部計算は丸めない。
- T6-K. 終了時刻より前に機体面を通過した障害物だけを数える。
- T6-L. フレーム間隔が`stallAbortMs=1000ms`を超えたら中断し、保存しない。1秒以下はdtで進める。
- 障害物は機体面をまたいだフレームで判定し、直後に最奥へ再配置する。描画対象は`z > collisionZ`だけ。投影はzが0以下を受け付けない。

### 既知の限界
- 当たり判定は機体中心の点なので、見た目の翼が障害物へ重なっても中心が開口内なら安全になる。
- Canvas描画の見た目と難易度はPCで確認した値であり、iPadの画面寸法・60Hz/120Hz・指によるスティック操作での体感調整は未実施。
- ブラウザが1秒以下で一時的に低fpsになった場合はdtどおり進むため、1フレームで障害物が大きく近づくことがある。ただし機体面をまたぐ判定は前後のzで行うため、衝突判定は飛ばさない。

### 次の初手
- レビュー承認後、`5eadf84`をpushし、Pagesのbuiltと公開ファイルを確認済み。
- ユーザーがiPad実機で3分通し、スティック操作と描画の体感を確認する。

## Phase1 テスト3(2026-09-22)

### ユーザー判断による変更(2026-09-22)
- 計算問題は、2桁(10〜20)を最大2つまでとし、残りを1桁(1〜9)にした。2桁の個数(0〜2)と位置を先に抽選し、各位置の範囲から構成的に生成するため、引き算に2桁が来ても問題全体を作り直さない。
- 音声は5語目の読み上げ後、回答するまで次の組へ進まない。`speechAnswerLimitMs` は残し、0を時間制限なしとして既定値を0にした。正数なら従来どおり時間切れを記録する。
- `audioUnanswered` は記録形式に残した。既存記録は変更しない。
- 単体テストは全体で106件。計算は1000シードで2桁が最大2つ、1桁が1〜9、全項が20以下、途中値が非負であることを確認した。音声は時間制限0で60秒待っても未回答にならず、回答の1000ms後に次組が始まることと、時間制限5000msなら従来どおり未回答になることを確認した。
- わざと2桁を3つまで許すと seed=4 で落ち、時間制限0でも5秒で未回答にすると60秒待機のテストで落ちることを確認してから戻した。
- Codex内ブラウザ(1180×820)で、2桁を2つ含む式の表示を確認した。5語目後に答えず20秒以上待っても同じ回答待ちのままで、回答後は次の組の1語目へ進んだ。コンソールエラーは0件。

### 完了
- `js/logic/t3.js`(純粋関数): 図形問題(直前と同じ並び・同じ向きは出さない)、計算問題と4択、音声の5語(重複は1組・隣り合わない)、音声タスクの進行(`stepAudio`)、声の選び方(`pickVoice`)、採点と記録。
- `js/tests/t3.js`: 3タスクを1つの rAF ループで回す。図形と計算は答えたらすぐ次へ。音声は `stepAudio` で時刻に沿って進める。押したボタンは最低 `answerFeedbackMs`(300ms)薄くする(正誤は出さない)。
- `js/core/landscape.js`(SPEC 外、承認済み): 縦のときは全面に「端末を横にしてください」を出し、開始ボタンを無効にする。T6 でも使う。
- 設定の型の確認に、配列(数値だけ)を追加した(`calcDistractorOffsets`)。
- 履歴の表は、内訳のうち `inHistory: false` のもの(回答数・正答率)を出さない。
- 単体テスト: 全体で 76 件(t3 は 25 件、設定の型の確認を storage に2件追加)。わざと壊すと落ちることを3種類で確認した(計算の途中の値が負・傾き±15°・重複2組)。
- ローカル(1180×820)で240秒を1回通して記録が保存され、履歴の表に出ることを確認した。

### 未解決
- 実機での確認(ユーザー待ち): 音が出るか、英語の声か、間隔、途切れ、マナーモード、縦の表示。
- この PC の Edge には英語の声がなく(日本語の声だけ)、声を指定しない(④)動作になった。1語の読み上げに約1.2〜1.7秒かかるため、「読み終えるまで待つ」(T3-A)により語の間隔は約1.4〜1.9秒に延びた。英語の声がある端末での間隔は実機で確かめる。
- レビュー承認済み。公開は次の手順1で行う。

### 仮定(承認済み。計画の T3-A〜L)
- T3-A. 語の間隔は、組の開始時刻を基準に読み始めを 1000ms ごとにする。前の語を読み終えていなければ待つ(読み上げは切らない)。
- T3-B. 重複する2語は隣り合わない位置に置く。
- T3-C. 重複を入れるかは組ごとに独立に抽選する(確率 50%)。
- T3-D. 読み終えて5秒答えなければ未回答(回答数に入れない)。次の組は回答と同じく 1000ms 後。
- T3-E. 終了時刻に残っていた問題・読み上げ中や回答待ちの組は数えない。
- T3-F. 正誤は出さない。答えたタスクはすぐ次の問題に変わる。
- T3-G. プレイ中に縦になったら中断し、保存しない。
- T3-H. 非表示・描画が止まった(フレームの間隔が `stallAbortMs`=1000ms を超えた)ときは中断し、保存しない。
- T3-I. 計算の誤答は正解 ±1・±2・±10 のうち、0以上で重ならないものから3つ。正解が0になってもよい。
- T3-J. 三角形の傾きは −10°〜+10° の一様乱数。傾けるのは三角形だけ。
- T3-K. 記録の内訳は各タスクの正答数・回答数・正答率(整数%、回答0なら null)と音声の未回答数。履歴の表は正答数と未回答数。
- T3-L. キーボード操作は付けない。
- 声の選び方: ① en-US で default の声 → ② en-US(おもしろ系を除き、localService を優先)→ ③ ほかの en-* の声 → ④ 声を指定せず lang='en-US' だけ。使う声は開始画面とプレイ中の上部にいつも出す。
- 開始ボタンの click の処理の中で、`speechSynthesis.cancel()` のあと、同期的に1語目の `speak()` まで進める(iOS 対策)。
- 読み終わりは `onend`。来ない場合は、読み始めから `speechEndFallbackMs`(2500ms)たつか、`speaking` と `pending` が false で読み始めから `speechIdleGraceMs`(250ms)たったら読み終わりとみなす。`onerror` も読み終わりとみなす。
- 計算の引き算は、選べるとき(途中の値が1以上)に確率 `calcSubtractRate`(0.5)で選ぶ。

### 既知の限界
- 計算の4択は、選択肢どうしの差から正解を割り出せることがある(誤答が正解 ±1・±2・±10 から作られるため)。SPEC の指定どおりなので直さない。
- 声の一覧が空の端末(iOS で起こることがある)では、声を指定せず lang だけで読む。その場合、どの声で読まれるかは端末しだい。
- 読み上げの速さや、1語にかかる時間は声によって違う。遅い声では語の間隔が 1000ms より長くなる(T3-A)。

### 次の初手
- レビュー後に push し、Pages のビルドを確かめる。
- ユーザーの実機(iPad・iPhone)で音声まわりを確かめてもらう。
- その後、Phase2(テスト6)に入る。

## Phase0(2026-09-22)

### 完了
- 土台: ハッシュルーティング(`#/` `#/t1`〜`#/t6` `#/history` `#/settings`)、メニュー(各テストの前回/最高/回数)、画面を切り替えるときの後片付け。
- `js/core/`: rng(mulberry32)、storage(apt_results は読み込みと追加だけ、apt_settings は読み込みだけ)、settings(t1〜t6 の既定値と保存値の合成)、stats(集計)、timer(rAF のループ)、result(共通の結果画面)、catalog(テスト一覧)。
- テスト2: `js/logic/t2.js`(生成・判定・採点。純粋関数)と `js/tests/t2.js`(描画と入力。図形は SVG)。
- 簡易履歴: テストを選び、直近20回を新しい順に表で出す。最高点(全記録)、直近5回の平均(小数1桁)、回数(全記録)も出す。
- 単体テスト: `node --test` で 49 件。わざと壊すと落ちることを3種類で確認した(誤押しの減点・連続上限・追加処理)。
- ローカル(1180×820)で、既定値の180秒を1回通して記録が保存され、履歴の表に出ることを確認した。
- 結果画面では「もう一度」にフォーカスを当てず、フォーカスを外す(終了直後のスペースキーで結果画面が消えないようにするため)。
- Phase0 の完了条件: ユーザーが PC と iPad の実機で、公開 URL のテスト2を最後まで遊べて記録が履歴に出ること、PC のスペースキーで押せることを確認した(2026-09-22)。

### テスト2の修正(2026-09-22、ユーザーの決定)
- 制限時間を 180秒 → 120秒 にした(`DEFAULTS.t2.durationSec = 120`。表示120回・一致30回)。SPEC §6 も変更。
- 左右が同じ表示を2回続けて出さないようにした(`maxConsecutiveMatches = 1`。k=1 で置ける一致の上限は floor(121/2)=60回)。生成の仕組みは変えていない。SPEC §6 も変更。
- 押したことが必ず見えるようにした。理由: これまでは「次の表示まで薄くする」だけだったため、表示の終わり近くで押すと薄い時間が短く、ほとんど見えなかった(計測: 表示の 902ms で押すと 131ms、965ms で押すと 83ms)。受け付けた押下のあとは、その表示が終わるまで、かつ押してから `pressFeedbackMs`(既定 300ms)たつまで薄くする。時間の管理は rAF のループの中で行う。正誤は出さない。薄いあいだでも新しい表示なら押下を受け付け、判定は押した時点の表示に対して行う。
- これまでの記録はそのまま残す(各記録に設定値が入っているので見分けられる)。

### 未解決
- テスト2の修正(120秒・連続なし・押下の表示)は、b369f6e で公開済み。
- ブラウザ操作ツールのキー送信は `key:"" code:""` のイベントになるため、ツールでのスペースキーの確認は `code:'Space'` の合成イベントで行う(実際のキーボードではユーザーが確認済み)。
- 設定画面・グラフ・既定値と違う回の印・書き出し/読み込み・全消去(Phase4)。
- T6 の初速・加速度・速度の上限・障害物の間隔は、SPEC に数値がないため既定値に入れていない(Phase2 で決める)。

### 仮定(承認済みの解釈と決定)
- A. 一致は連続させない(`maxConsecutiveMatches=1`)。2026-09-22 にユーザーの判断で「3回以上続かない(既定 2)」から変更した。
- B. 一致の回数は N×確率 に固定し、構成的に並べる(不一致 N−m 個の隙間 N−m+1 か所に、各 k 個までの容量で一致を1つずつランダムに配る)。棄却サンプリングは使わない。
- C. 直前とまったく同じ表示は出さない(候補から外して選ぶ)。
- D. 押したときに正誤は出さない。ボタンを薄くするだけ(その表示が終わるまで、かつ押してから最低 `pressFeedbackMs`)。
- E. プレイ中にページが非表示になったら中断し、保存しない。さらに、描画が止まって表示が1つ以上飛んだとき(i > cur + 1)と、最後の表示を描く前に終了時刻を過ぎたときも、中断して保存しない。メニューに「描画が止まったため中断しました(記録は保存していません)」と出す。
- F. 同じ id の記録は追加しない。
- 1. 履歴に「直近5回の平均」を出す(5回未満ならある分だけ、小数1桁)。表は直近20回を新しい順。最高点と回数は全記録から。集計は `js/core/stats.js`。新旧は配列の順ではなく `date` で決める。
- 2. Phase0 では削除の関数を作らない。Phase4 で全消去の関数を1つだけ例外として追加する。
- 3. 保存に失敗したとき(apt_results が壊れている・容量超過など)は、点数と内訳を出したうえで「保存できませんでした(理由)」と表示する。再試行はしない。
- 4. apt_settings が JSON として読めないときは既定値で動かし、メニュー上部に注意を出す。apt_settings は書き換えない。型が合わないキーだけ既定値に戻す(NaN・Infinity も不可)。
- 5. 的中0回のときの平均反応時間は、保存値 null、表示「—」。値があるときは整数ミリ秒。
- 6. 一致の回数 m = Math.round(N × 一致確率)。
- 7. 必要な不一致の数は ceil(m/k)−1。置ける一致の最大数は floor(k(N+1)/(k+1))(N=180, k=2 なら 120)。超えたらエラーにし、使える確率の上限(例: 0.667)をエラー文に入れる。
- 8. 終了は N×間隔の時点。残り時間も N×間隔から数える。
- 9. 点数 = Math.round(Math.max(0, hits*100/(hits+misses) − fa*penalty))。hits+misses=0 なら 0。
- 表示の件数(直近20回・直近5回)は `settings.js` の `DISPLAY` に置いた(利用者が変える値ではないので apt_settings には入れない)。

### 既知の限界
- 反応時間の起点は、その表示を描いたフレームの rAF 時刻。実際に画面に出るのはその後なので、反応時間は最大で約16ms(1フレーム)長めに出る。
- 表示が切り替わった直後に処理された押下は、前の表示のあいだに押したものでも次の表示のものとして数えられ、反応時間は 0ms に切り上げられる(`Math.max(0, ts - frameTs)`)。起きる時間の幅は1ms程度でまれなので、直さない。
- 「描画が止まったため中断」の処理(E の拡張)は、狙って確かめる方法がない。ただし 2026-09-22 に、ブラウザ操作ツールの画面が非表示になって描画が止まったとき、実際にこの中断が働いた(メニューに「描画が止まったため中断しました(記録は保存していません)」が出て、記録は増えなかった)。

### 次の初手
- テスト2の修正をレビュー後に push する。
- Phase1(テスト3)の計画をレビュー・承認してから実装に入る。
- 公開済み(2026-09-22): リポジトリ https://github.com/haxitas/aptitude-practice (公開、main の / から Pages)、URL https://haxitas.github.io/aptitude-practice/ 。公開 URL の index・js・css が 200 で返り、PC のブラウザでメニューに6テストが出て、コンソールにエラーがないことを確認した。
# 2026-09-23 実機確認後の修正: テスト1

- ユーザー決定: 9種類へ拡張(割合の逆算・単価と合計・平均・経過時間)。種類は等確率、直前と同じ種類は候補から除外する既存の連続同問防止を維持。
- 問題文の小数は第1位まで。周回距離は速度和×速度差×倍率が6で割り切れる組を構成し、80%は60で割り切れる整数距離の組から選ぶ。既存の速度・倍率設定で整数距離候補がなくなる場合は設定保存を拒否。
- 4択は正の整数・重複なし・桁ずらし最大1つを維持。新しい範囲設定はcore/settings.js、入力欄はsettings-form.jsへ追加。過去の成績は変更しない。
- 先行テスト: 15件中3件失敗(9種類不足、0.833333km)。実装後15件成功。故障注入で距離の小数桁制限を外すと2件失敗(seed24:1.833333km)、復元後は全173件成功。
- ブラウザ: 1180×820で9種類の問題と整数4択を確認。390×844でも問題文と4択が画面内に収まる。ブラウザ回答は確認用のスクリプト操作、途中終了で成績保存なし。
- 公開済みPhase4: fe58324までpush、Pages built、js/settings.jsとjs/logic/history.jsはHTTP200/application/javascript。今回の修正はレビュー後にpushする。
# 2026-09-23 実機確認後の修正: テスト3

- ユーザー決定: 縦画面も許可。縦は図形・計算・音声を3段、横は従来配置。回転による中断を外し、非表示・描画停止による中断は維持。
- 開始画面の余白を含めてタップで開始(メニューリンクは除外)。説明3行と「本来は横画面…」を表示。最初のspeakはタップの同期処理内に残す。
- speechIntervalMsを廃止し、speechGapMs=500へ。onendの時刻から待つ。onendなしの打ち切り・idle検出を維持し、終了とみなした時刻からも500ms待つ。5語目のあとは待ち間隔なしで回答可能。古い記録は変更しない。
- 先行テスト: 28件中5件失敗。実装後28件成功。故障注入で500ms待ちを外すと4件失敗し、復元。開始イベント内のspeak呼び出しもfake音声のUI境界テストで確認。
- ブラウザ: 390×844の開始画面の下部余白をタップすると1/5から読み上げ進行が始まり、3段が画面内に収まる。1180×820に回しても中断せず従来の左/右上/右下になり、5/5まで進行。コンソールerrorなし。この環境の声一覧は空のため、実際の聞こえ方はiPad確認へ。
- SPEC §5と§6音声間隔・開始タップをユーザー決定に更新。§5のT6縦画面対応は後続のテスト6修正commitで実装する。
# 2026-09-23 実機確認後の修正: テスト4

- ユーザー決定: 本番前に機首N・相対90°の例題を表示。西のマスとNの向きを選択表示し、塔は東・自機は反対側という解説を出す。計器・解答欄の描画は本番と共用。
- 例題中はタイマー・回答集計を作らない。メニュー以外のタップで本番へ。共通タイマーに任意のstartAtを追加し、T4だけタップのperformance.now()を渡す(他テストの開始基準は変えない)。
- 先行テスト: 例題のexportなしと、タイマーが最初の描画を起点にして250msを0msとする失敗を確認。故障注入で例題の相対方向を90°から180°にすると1件失敗。復元後は全177件成功。
- ブラウザ: 1180×820と390×844で同じ例題・西/Nの強調・説明が画面内に収まる。例題を眺めている間は時計なし。タップで例題が消えて本番へ、ブラウザに既存保存されていた10秒設定で0:10から開始することを確認。
# 2026-09-23 実機確認後の修正: テスト5

- ユーザー決定: 再配置の点をdotMoveMs=400msでスライドさせる。旧点を順に未使用の最寄り新点へ結ぶ貪欲法(同距離は添字順)と線形補間を純粋関数に分離。個数の分布・配置生成は変更なし。
- 共通タイマーの1本のrAF内で毎フレーム描画。スライド時間を再配置間隔より長くした場合も、現在表示している途中位置から次の移動を始める。次問では移動状態を破棄。
- 先行テスト: 補間関数のexportなしで失敗。故障注入で補間率を常に1にすると位置テスト失敗、復元後は全180件成功。
- ブラウザ: 1180×820の同じ第1問・残り2:53の連続4枚で点が段階的に移動することを確認。390×844でも円と3〜13ボタンが画面内。コンソールerrorなし、途中終了で記録なし。
- 既知の限界: 配置の始点・終点は最小間隔を守るが、貪欲対応の直線移動では途中で点が近づく場合がある。点は移動中も円の中にある。
# 2026-09-23 実機確認後の修正: テスト6

- ユーザー決定: 横画面は操縦円を右(左右切替可)、縦画面はトンネル上・操縦円下。横画面の強制・回転による中断を削除。非表示・描画停止は引き続き中断。
- stickSide='right'を既定値に追加。開始前と本番の切替ボタン、設定画面の右/左選択からapt_settings.t6.stickSideへ保存。切替時だけ本番にも即反映(ほかの設定は従来どおり次回から)。保存失敗時は元の側を維持し理由を表示。過去の記録は変更しない。
- 配置は純粋関数。画面全体の短辺×0.3以上のトンネル半径、60px以上の操縦半径を確保。帯を除いた実際のCanvas領域を準備/本番共通のshellで測定し、小さすぎると理由を出して開始不可。回転・左右切替ではキー/ポインター入力とcaptureを解除。
- 先行テスト: 関連47件中7件失敗(左右未対応、横上下配置、縦の大きさ、48pxの操縦半径、小画面を許可)。故障注入で左設定を無視すると1180×820左の検査が失敗、復元。左右の保存で成績が1文字も変わらないこと・保存失敗時に設定と側を維持するUI境界テストも追加。全186件成功。
- ブラウザ: 1180×820で右→左へ切替、390×844で上下配置のまま継続、844×390でも帯・2円が画面内。再読込後も左を維持し、設定画面の項目も左。設定画面で右へ保存すると次回の準備画面も右。240×200では理由とdisabledの開始ボタン、390×844へ戻すと開始可能。縦からの開始も確認、コンソールerrorなし。
- 今回のブラウザ確認は短時間の動作確認。T4は既存10秒設定で結果保存(0点/回答0)、その他は途中終了。既定時間の完走は前フェーズで実施済み。iPadの実音・指の操作感はユーザー確認へ。

# 2026-09-23 実機確認後の修正: テスト4の計器の流儀

- ユーザー決定: 左の計器だけを `northUp`(北が上・飛行機形の針が機首、従来方式) / `noseUp`(機首が上・針が北)から選べるようにし、既定は `noseUp`。針の角度はそれぞれ `機首index×45°` / `(360°−機首index×45°) mod 360°`。右のADF、問題生成、正解計算、採点は変更しない。
- 設定画面に `compassMode` を追加。開始画面・例題に選択中の流儀を表示。例題は `noseUp` で機首W・ADF真上→自機E/W、`northUp` で機首NE・ADF右90°→自機NW/NE。誤答時は次問の下に、選択中の流儀による針の読み方から自機の位置まで3段階で説明する。従来の記録は変更せず、新しい記録の `settings` に流儀を残す。
- 先行テスト: `compassNeedleAngle` 未実装と `compassMode` 既定値なしで2件失敗。実装後、角度の全8方位・図の文字/印・例題/解説・設定検証を含む全194件が成功。故障注入で `noseUp` の角度を機首と同じ方向にすると、角度・SVG・解説の3件が失敗し、復元後に全件成功。
- Codex内ブラウザ: `noseUp` 例題でW/ADF真上・針90°・Eのマス/Wの向き・3段階の解説を確認。`northUp` 例題でNE/ADF右90°・針45°・飛行機印・NW/NEを確認。両流儀で誤答後の説明を表示し、10秒設定の `northUp` 結果は0点/回答1/向きだけ正解1で保存された。保存JSONの `settings.compassMode` は `northUp`。設定は確認後に既定値へ戻し、コンソールerrorは0件。iPad実機では両流儀の針と文字の読みやすさ、解説の理解しやすさを確認する。
- 未解決: この修正はレビュー待ち。承認後にpushする。

# 2026-09-23 レビュー後の修正: テスト4の共通例題

- 2つの流儀で例題を機首NE・ADF右90°・塔SE・自機NWのマス/向きNEに統一し、違いは針の読み方を説明する文章だけにした。直前の節に記した `noseUp` の機首W・ADF真上は採用しない。
- 45°刻みでは機首とADFが両方斜めだと塔の方位は縦横になるため、機首と塔を斜めにし、ADFは横向きの例を採用した。
- 先行テストは旧例題と旧説明で2件失敗。故障注入で例題の機首をEにすると例題テストが1件失敗し、復元後にテスト4の17件が成功。問題生成・正解計算・採点は変更しない。

# 2026-09-23 実機確認後の修正: テスト4の計器と練習

### 完了
- 計器周囲の流儀の説明と角度数字の目盛りを外し、両計器に薄い4本の直径線で8等分の目安を付けた。コンパスは中心を挟む赤/淡色の1本の菱形針、ADFは青の一方向針。北の文字はnoseUpの針先だけ、方位文字はnorthUpだけに残した。
- マスと向きの両方を選ぶと、選択マスに✈を表示する。回転は `方位index×45°−planeGlyphBaseDeg` とし、既定の基準角45°を設定画面で変更可能にした。例題・練習・本番で同じマス表示を使う。
- 例題の開始は「始める」ボタンのみ。左に「練習問題を解く」を置いた。練習は3問、無制限、成績保存なし。回答の次画面に自分の答えと正解・正解位置の✈・角度数字のない3行の導き方を表示する。3問後に再練習/本番開始を選べる。再練習の最初も直前と同じ問題を避ける。
- 先行テストは未実装の練習関数と基準角設定で失敗し、練習の再開始時の重複防止も追加前に失敗した。故障注入で飛行機の回転式の減算を加算にすると対象テスト1件が失敗し、復元した。最終の全テストは198件成功。
- Codex内ブラウザ: 1180×820/noseUp、390×844/northUpで練習3問を最後まで操作し、各問の解説・結果の2ボタン・再練習で1問目に戻ることを確認。逆の組み合わせの例題も撮影。余白タップでは開始せず、「始める」/「本番を始める」の直後に3:00から時計が動いた。選び直しで✈がE/−45°からW/45°へ移り、結果の正解マスにも✈が出た。練習の前後でメニューのテスト4の記録回数は10回のまま。コンソールerrorなし。確認用の流儀・画面寸法は既定に戻した。

### 未解決・仮定・次の初手
- ✈は端末のフォントごとに元の向きが異なる。既定45°はユーザー指定値で、このCodex内ブラウザでは無回転時に右向き寄りに見える。iPadの実機で表示を確認し、必要なら基準角を設定画面で調整する。
- 今回の修正はローカルcommitまで。レビュー後にpushする。

# 2026-09-23 実機確認後の再修正: テスト4の飛行機・本番表示・固定文字盤

### 完了
- フォント依存の✈を、機首が上のSVG飛行機に置き換えた。回転は方位index×45°（N=0°）。`planeGlyphBaseDeg` と設定画面の項目は削除した。過去の記録は変更しない。
- 本番中の前問の正解・○×と、そのためだけの状態/関数を削除した。`showPreviousAnswer` と設定画面の項目も削除。解説は無制限の3問練習だけに残す。
- コンパスの8方位文字と8等分の線を両流儀で同じ画面座標に固定し、回すのは針だけにした。noseUpは上の機首印と赤い北針、northUpは機首印なし・赤い機首針。針先の「北」は削除。noseUpの機首印はN文字と重ならないよう円の上端の三角形にした。針角の式と問題生成・採点は変更しない。
- テスト先行: 飛行機SVGの未exportでT4テストが、設定項目の残存でsettings-formテストが失敗。機首印の重なりは新たなテストを先に追加し、旧text印で失敗してからpath印へ変更した。故障注入で飛行機の角度を+45°するとN=0°の検査が `45 !== 0` で失敗し、復元した。
- Codex内ブラウザ: 1180×820と390×844、両流儀の例題を確認。noseUp本番で8方位を選ぶとSVGは0/45/90/135/180/225/270/315°に回転。19問目の機首Wで赤針先は(162,100)、右(E)の位置。問題をまたいで8文字の座標は一致。本番で回答後、前問の正解・○×は表示されなかった。縦画面の計器は各177×177pxで、スクリーンショットも確認した。

### 未解決・仮定・次の初手
- iPadでSVG飛行機の向きと、小さい画面での文字盤の読みやすさを実機確認する。今回の変更はローカルcommitまで。レビュー後にpushする。

# 2026-09-23 実機確認後の修正: テスト4の補助線

### 完了
- 左右の計器の8等分線を共通の境界色のまま1px/不透明度0.38から1.5px/0.7へ変更。線は針・文字の下に描く。
- CSSの値を確かめるテストを先に追加し、旧値で失敗。故障注入で不透明度を0.38へ戻すと同じテストが失敗し、復元。
- Codex内ブラウザで1180×820と390×844の暗い配色を撮影。明るい配色は暗色メディアルールを一時的に無効にして同じ2寸法を撮影し、ルールを元に戻した。両配色で針・文字より下の線を確認した。

### 未解決・仮定・次の初手
- iPad実機で補助線の濃さを確認する。pushはレビュー後。

# 2026-09-23 実機確認後の修正: テスト5の前問表示

### 完了
- 回答または10秒の時間切れで次問へ進んだとき、画面下に前問の正解個数・自分の答え（未回答は「なし」）・○×を小さく表示する。初問では表示しない。表示文の生成は純粋関数。
- 先行テストは関数の未exportで失敗。故障注入で○×を逆にすると正解ケースの比較が失敗し、復元した。
- Codex内ブラウザで1180×820と390×844を確認。回答後の「正解7個/答え3個 ×」と、時間切れ後の「正解13個/答えなし ×」を確認し、円と数字ボタンを隠さない。既定180秒の結果は0点・回答1・未回答17で保存された。

### 未解決・仮定・次の初手
- iPad実機で小さい表示の読みやすさを確認する。pushはレビュー後。

# 2026-09-23 実機確認後の修正: テスト6の不透明な障害物・回転する扇形・羽根の開口数

### 完了
- 障害物は既存の描画対象数を減らさず、奥から手前へ不透明色で塗る。扇形・小穴・羽根の開口は偶奇塗りつぶしで抜き、奥を見通せる。手前の塞がった部分は奥の障害物を隠す。
- 扇形にはランダムな回転方向を持たせ、羽根と同じ初期角速度・角加速度で回転。難易度の上がり方をそろえ、設定値を余計に増やさないため。毎フレーム更新した開口中心を当たり判定と押し戻し方向の両方に使う。
- 羽根の開口数は設定値 `bladeOpeningCounts=[1,2,3]` から等確率で選び、各開口を360°/個数の間隔に配置。中心の小円は従来どおり衝突。
- 先行テストは既定値・羽根の開口数と配置・扇形の回転・設定画面の項目が未実装で4件以上失敗。故障注入で扇形の回転更新を止めると回転テストが失敗し、復元した。
- Codex内ブラウザ: 390×844で既定180秒を完走し、17点・衝突36・距離195u・最高速度1.6u/s、「記録を保存しました」、コンソールerrorなし。1180×820でも不透明な手前の障害物が奥を隠し、開口の背後にトンネルが見える画面を撮影した。羽根開口数1〜3と扇形の両回転方向は単体テストでも確認した。

### 未解決・仮定・次の初手
- 「2個まで」は障害物の描画数を2個へ制限する意味には取らず、明示された「描く数は減らさない」を優先した。iPadで不透明な重なりと開口の読みやすさを確認する。pushはレビュー後。

# 2026-09-23 電卓の追加

### 完了
- 純粋関数の電卓を追加。×・÷を優先し、連続した演算子は最後を採用。=の反復は直前の演算子と右辺を繰り返す（複合式でも最後の演算を使う）。内部は倍精度を維持し、表示だけ小数第3位へ四捨五入する。12桁超の結果は指数表記。
- `#/calc` をメニューに置き、テスト1の開始・結果画面からも開ける。テスト1のプレイ中は電卓を既定で表示し、設定で非表示にできる。電卓操作は成績を保存・変更しない。
- テスト先行で電卓モジュール未実装と電卓設定未実装の失敗を確認。故障注入で小数表示を整数丸めにすると小数テストが、演算子の優先順位をなくすと `2+3×4=14` のテストが失敗し、両方を復元した。
- Codex内ブラウザ: `#/calc` でボタン入力 `2+3×4=14`、キーボード入力 `6÷3=2` を確認。テスト1開始画面から電卓へ移動でき、1180×820と390×844のプレイ画面では問題・選択肢・電卓が重ならない。プレイ中のキーボード `2+3=` は電卓だけを更新し、問題は進まない。電卓操作の前後で設定画面から書き出した成績の `records` 5件が完全一致した。`node --test` は213件成功。

### 未解決・仮定・次の初手
- 電卓の入力中も表示は小数第3位までだが、内部の入力と計算は丸めない。画面確認後、iPad実機で電卓の打ちやすさを確認する。

# 2026-09-23 テスト6: 小穴と開口部の縁を改訂

### 完了
- 小穴の候補位置を上下左右の4か所へ固定し、半径を0.3へ拡大。開口数1〜3を等確率で選び、1穴は静止、2・3穴は既定50%で羽根・扇形と同じ角速度でランダム方向に回る。旧保存設定の `holeSlotCount=8` は使わず4へ戻し、旧 `holeOpenCount` は無視する。過去の成績は変更しない。
- 半円の直径、羽根と扇形の開口辺、小穴の円周に明るい輪郭を描いた。塗りは従来の不透明のまま、奥の障害物を手前で隠す。
- 先行テストは旧既定値・斜め配置・開口数固定・回転未実装で失敗。故障注入で穴を斜めにも置くと位置テストが、1穴を回転させると静止テストが失敗し、両方を復元した。
- Codex内ブラウザ: 390×844で既定180秒を完走し、17点・衝突36・距離194.7u・最高速度1.6u/s、「記録を保存しました」、コンソールerrorなし。1180×820では3穴の上下左右配置と明るい穴の輪郭を撮影し、縦画面では扇形・羽根・半円の縁線を撮影した。1穴の静止、2・3穴の回転/静止は単体テストで確認した。

### 未解決・仮定・次の初手
- 穴の「上下左右」は回転前の4候補位置を指す。回転する2・3穴は経過時間により斜め方向も通る。iPad実機で開口の見やすさと操作感を確かめる。pushはレビュー後。

# 2026-09-23 テスト5: 開始直後の移動と回答待ち

### 完了
- 点の再配置は問題開始後の最初の描画フレームで始め、1000ms・2000msで次の再配置へ進む。補間400msと点数・配置条件は変更しない。
- 1問の制限時間は既定0(無制限)とし、回答まで同じ問題が動き続ける。正数に設定した場合の時間切れ処理と全体終了優先は残した。前問の正解は回答した場合だけ表示し、未回答のときは表示しない。
- 先行テストは旧既定値・初回移動の遅れ・未回答処理・前問表示で失敗。故障注入で初回移動を1秒遅らせると `0 !== 1`、制限時間0を通常の期限として扱うと `true !== false` で失敗し、復元した。
- Codex内ブラウザ: 1180×820で開始直後と移動後の点配置を撮影。34秒以上回答を待っても第1問のままで、回答後に第2問へ進み「前の問題の正解: 8個 / あなたの答え: 8個 ○」が出た。390×844でも円・ボタン・前問表示の配置を撮影した。追加確認の回は途中終了し、成績を保存していない。

### 未解決・仮定・次の初手
- 旧端末に保存された `questionLimitSec=10` の上書きは利用者の設定として残し、既定値だけ0へ変更する。iPad実機で動き出しと長時間の回答待ちを確認する。pushはレビュー後。
