# SPEC: 適性検査 練習サイト(テスト1〜6)

## 0. 目的と成功の定義
- 航空系の3次試験で出る適性検査(テスト1〜6)を、スマホ・PC・iPadの1つのURLから練習できるようにする。
- 合否判定はしない。各テストの点数を記録し、過去の成績の推移を簡単に見られればよい。
- 成功 = (a) 6テストすべてが実機(iPad横画面・PC)で最後まで遊べる (b) 終了ごとに点数が保存され、履歴画面で直近の推移が見える (c) 制限時間・切り替え間隔などの数値を設定画面から変えられる(体感で調整するため)。
- 期限: 本番まで1週間。開発は2〜3日で打ち切り、残りは練習に使う。

## 1. 非目標(作らない)
- 合否判定・偏差値・他人との比較
- サーバー、ログイン、DB、有料サービス(¥0維持)
- 端末間の成績同期(成績は端末ごとに保存。代わりにJSON書き出し/読み込みを用意する)
- 3Dライブラリ(three.js等)・フレームワーク・ビルド工程
- 音声ファイルの録音・同梱(ブラウザの音声合成で代用する)

## 2. 制約
- 開発: Claude Code。公開: GitHub Pages(無料)。
- 端末: iPad・PC(Inspiron 14)を主、iPhoneは横画面で動けばよい。
- 素の HTML + CSS + JavaScript(ES Modules)。依存パッケージなし。単体テストは `node --test`。
- 日常運用: URLを開く → テストを選ぶ の2アクション。

## 3. アーキテクチャ
- 1ページのアプリ(index.html)。ハッシュで画面を切り替える: `#/`(メニュー) `#/t1`〜`#/t6` `#/history` `#/settings`
- ロジックとDOMを分離する。問題生成・正誤判定・採点・衝突判定は DOM に触れない純粋関数として `js/logic/` に置き、node で単体テストする。描画と入力は `js/tests/` 側。
- 乱数は種(シード)を渡せる自前の関数を使う(テストで結果を固定するため)。
- 時間管理は `requestAnimationFrame` と `performance.now()` を基準にする。`setInterval` の累積ずれに依存しない。

### リポジトリ構成
```
index.html
css/style.css
js/app.js              # ルーティング・メニュー
js/core/storage.js     # 成績(append-only)と設定の保存
js/core/settings.js    # 既定値と設定画面
js/core/timer.js       # 残り時間表示・終了処理の共通部品
js/core/rng.js         # シード付き乱数
js/core/result.js      # 共通の結果画面
js/logic/t1.js … t6.js # 各テストの生成・判定・採点(純粋関数)
js/tests/t1.js … t6.js # 各テストの描画と入力
js/history.js          # 履歴画面
test/*.test.js         # node --test 用
CLAUDE.md
PHASE_NOTES.md
```

## 4. データ
### 成績(localStorage キー `apt_results`、append-only)
```json
{"records":[
  {"id":"2026-09-23T10:15:00.000Z-t2","test":"t2","date":"2026-09-23T10:15:00.000Z",
   "score":72,"detail":{"hits":36,"misses":9,"falseAlarms":4,"meanRtMs":512},
   "settings":{"durationSec":180,"intervalMs":1000,"matchRate":0.25}}
]}
```
- 追加のみ行う。既存要素の書き換え・削除をするコードを書かない(全消去はユーザーが設定画面で2段階確認したときだけ)。
- 各記録にその回の設定値を保存する。体感で設定を変えた前後の点数を区別できるようにするため。
- 設定画面に「成績をJSONで書き出す」「JSONを読み込む(重複idは無視して追加)」を置く。

### 設定(localStorage キー `apt_settings`、上書き可)
- テストごとの数値を保存する。キーがなければ §6 の既定値を使う。

## 5. 共通の画面仕様
- メニュー: 6テストのボタン。各ボタンに「前回の点数 / 最高点 / 回数」を小さく表示する。
- 各テスト: 開始画面(説明1〜2行+開始ボタン)→ 本番(上部に残り時間)→ 結果画面(点数・内訳・「もう一度」「メニュー」)。
- 途中終了ボタンを置く。途中終了した回は保存しない。
- ③と⑥は本来は横画面で行うが、縦画面でも遊べる。③は縦のとき図形・計算・音声を縦に並べ、⑥はトンネルを上・操縦の円を下に置く。⑥で必要な大きさを確保できない画面では理由を出して開始できないようにする。(2026-09-23 ユーザーの判断で変更)
- ボタンはタップしやすい大きさにする(最低44px)。ダブルタップによる拡大を無効化する(`touch-action: manipulation`)。

## 6. 各テストの詳細仕様(数値はすべて設定で変更可、下記は既定値)

### テスト1 計算(4択)
- 制限時間 180秒。回答するとすぐ次の問題に進む。
- 問題はテンプレートに乱数を入れて生成する。
  - 単位換算: ha↔m²(1ha=10,000m²)、a↔m²、km²↔ha、km↔m、時間↔分
  - 速さ・時間・距離: 「時速2kmで4時間進むと何km?」など
  - 出会い・追いつき: 周囲L kmの池を時速a, bで逆方向(出会い=L/(a+b))または同方向(追いつき=L/|a−b|)に進む。答えは「分」で出す
  - 割合: 「○○の△%は?」
- 答えが整数(分・m²など)になる数値の組だけを採用する。例: L=4.2, a=4, b=3 → 36分。
- 誤答の選択肢は典型的な間違いから作る: 桁を1つずらす、a+bとa−bの取り違え、時間と分の換算忘れ。4つの選択肢が重ならないようにする。
- 点数 = 正答数。内訳として回答数と正答率を出す。

### テスト2 同一図形の検出
- 左右に図形を大きく表示する(△ ★ ○ □ ◇ ☆ の6種類から)。表示は 1000ms ごとに左右同時に切り替わる。
- 左右が同じ組になる確率は 25%。同じ組が2回続けて出ないようにする(2026-09-22 ユーザーの判断で変更)。
- 下部に大きな「同じ」ボタン(キーボードならスペースキー)。
- 判定: 同じ組の表示中に押す → 的中(反応時間を記録)。同じ組で押さない → 見逃し。違う組で押す → 誤押し。1回の表示中に押せるのは1回まで。
- 制限時間 120秒(2026-09-22 ユーザーの判断で変更)。
- 点数 = max(0, 的中率% − 誤押し数×2) を四捨五入。内訳: 的中・見逃し・誤押し・平均反応時間。

### テスト3 マルチタスク(3つのタスクを同じ画面で同時に行う)
- レイアウト: 左半分=図形タスク、右上=計算タスク、右下=音声タスク。3つは独立していて、答えたタスクだけが次の問題に進む。
- 制限時間 240秒。
- 図形タスク(左): ○ × □ ▷(または◁)の4つを、横一列にランダムな順番で並べる。三角形は右向きか左向きのどちらかで、±10°以内のわずかな傾きを加える。下の「◀」「▶」ボタンで三角形の向きを答える。
- 計算タスク(右上): 4つの数の加減算(例: 13+8−4+2)。2桁の数(10〜20)は最大2つまでとし、残りは1桁(1〜9)にする。2桁の数の個数(0〜2)と位置は問題ごとにランダムに決める。(2026-09-22 ユーザーの判断で変更) 途中の値が負にならないようにする。答えは4択で、誤答は正解±1、±2、±10などから作る。
- 音声タスク(横画面では右下): NATOフォネティックコード26語(Alfa, Bravo, Charlie, Delta, Echo, Foxtrot, Golf, Hotel, India, Juliett, Kilo, Lima, Mike, November, Oscar, Papa, Quebec, Romeo, Sierra, Tango, Uniform, Victor, Whiskey, X-ray, Yankee, Zulu)から5語を1回だけ読み上げる。次の語は前の語を読み終えてから speechGapMs(既定500ms)後に読む。(2026-09-23 ユーザーの判断で変更)
  - 50%の確率で、ちょうど1組の重複を入れる(例: Alfa, Sierra, Delta, Romeo, Alfa)。
  - 5語目を読み終えたら「重複あり」「重複なし」ボタンを有効にし、答えるまで待つ。押してから1000ms後に次の5語を開始する。(2026-09-22 ユーザーの判断で変更) 設定値で回答時間を正数にした場合は、その時間で未回答として次へ進む。
  - 読み上げは Web Speech API(`speechSynthesis`)を使う。言語は en-US、速さは 0.9。英語の音声がない端末では、使える音声で代用して画面に注意を出す。
  - iOSでは、最初の読み上げをユーザーの操作の中で起動しないと音が出ない。そのため開始画面のタップの処理の中で最初の読み上げを始める。(2026-09-23 ユーザーの判断で変更)
- 点数 = 3タスクの正答数の合計。内訳: タスクごとの正答数・回答数・正答率、音声の未回答数。

### テスト4 計器から自機の位置と向きを読む
- 左の計器: コンパス。文字盤は画面に固定し、N・NE・E・SE・S・SW・W・NWの8方向を表示する。既定は「文字盤の上が機首・赤い針が北」で、設定から「Nが上・赤い側が機首」に切り替えられる。回るのは針だけ。(2026-09-23 ユーザーの判断で変更)
- 右の計器: 相対方位の計器(ADF型)。機首を上とした円盤の上で、針が電波塔の方向を指す。相対角度も8方向(45°刻み)。
- 正解の計算: 塔の方位 = (機首の方位 + 相対角度) mod 360。自機は、塔から見てその反対方向のマスにいる。
- 解答欄: 3×3マス(中央は電波塔で選べない)。①マスをタップして位置を選び、②8方向の矢印ボタンで機首の向きを選び、③「決定」を押す。
- 検証用の例(単体テストに必ず入れる): 機首N・針が右 → 自機は西のマス・向きN。機首W・針が真上 → 自機は東のマス・向きW。
- 制限時間 180秒。1問ごとの時間制限はなし。
- 点数 = 位置と向きの両方が正しい問題の数。内訳: 位置だけ正解、向きだけ正解。

### テスト5 点の数を数える
- 大きな円の中に点を3〜13個表示する。1問の中では点の数は変わらず、1000msごとに位置だけが入れ替わる。
- 点どうしが重ならないように最小間隔を設け、すべての点を円の内側に置く。
- 解答: 3〜13の数字ボタン(11個)。1問の制限時間は10秒で、過ぎたら未回答として次へ進む。
- テスト全体の制限時間 180秒。
- 点数 = 正答数。内訳: 回答数・未回答数・平均の誤差(|回答 − 正解| の平均)。

### テスト6 トンネル飛行
- 視点: 機体の後方から、円形のトンネルの奥を見る。Canvas 2D の疑似3Dで描く(奥行きzの物体を `scale = f / z` で縮小して描く)。
- 操作: 矢印キーを押している間、または画面の操縦用の円を指・マウスで動かしている間、入力方向へ移動する。両方の入力は合成し、離すとすぐ止まる。機体はトンネルの内側からはみ出さない。(2026-09-23 ユーザーの判断で変更)
- 障害物は次の4種類を同じ確率で出す。(2026-09-23 ユーザーの判断で変更)
  - 半円型: 円の半分がふさがっている。ふさがる側は上・下・左・右からランダム。
  - 回転する羽根型: 60°の開口部が1〜3個あり、個数は等確率で選ぶ。開口は等間隔に配置する。回転の向きはランダムで、速さは時間とともに上がる。中心の小円はふさがっている。(2026-09-23 ユーザーの判断で変更)
  - 扇形: 円の4分の1(90°)だけが開いている。開口の初期中心は45°・135°・225°・315°のいずれかで、羽根と同じ速度で時計回りまたは反時計回りに回転する。(2026-09-23 ユーザーの判断で変更)
  - 縁の小穴: 円全体がふさがり、縁寄りに重ならない小さい丸穴が3か所開いている。
- 障害物のふさがった部分は不透明に描く。奥から手前の順に重ね、開口部は塗らず、奥が見えるようにする。描く障害物の数は減らさない。(2026-09-23 ユーザーの判断で変更)
- 当たり判定: 障害物が機体の奥行きを通過する瞬間に、機体の断面上の位置(極座標の半径と角度)が開口部の中にあるかを判定する。純粋関数として実装し、単体テストする。
- 衝突したら: 機体を開口部の方向へ押し戻す演出を入れ、速度を50%に落とす。その後、再び加速する。
- 速度: 初速から時間に比例して上がり、上限を設ける。
- 制限時間 180秒。
- 点数 = 通過した障害物の数。内訳: 衝突回数・進んだ距離・最高速度。

## 7. 履歴画面(`#/history`)
- テストを選ぶと、直近20回の点数を折れ線グラフ(Canvasで自作)と表(日時・点数・主な内訳)で表示する。
- 最高点・直近5回の平均・回数を表示する。
- 設定値が既定と違う回には印を付ける(設定変更の前後を見分けるため)。

## 8. 設定画面(`#/settings`)
- §6 の各数値(制限時間、切り替え間隔、同じ組の確率、読み上げ間隔と速さ、1問の制限時間、⑥の初速・加速度など)をテストごとに編集できる。
- 「既定値に戻す」ボタン、成績の書き出し・読み込み、成績の全消去(2段階確認)を置く。

## 9. フェーズ計画(1フェーズ=1セッション)
| Phase | 内容 | 完了条件(証拠つき) |
|---|---|---|
| 0 | 土台(ルーティング・保存・タイマー・結果画面・簡易履歴の表)+ テスト2 + GitHub Pagesで公開 | iPadでURLを開き、テスト2を最後まで遊べて、記録が履歴の表に出る |
| 1 | テスト3 | iPadの横画面で3タスクが同時に動き、iPhoneとiPadで音声が鳴る |
| 2 | テスト6 | iPadで指の操作で3分遊べる。当たり判定の単体テストが通る |
| 3 | テスト4・5・1 | 3テストが最後まで遊べる。④の検証用の例が単体テストで通る |
| 4 | 履歴グラフ・設定画面・書き出し/読み込み | 設定を変えるとテストに反映され、履歴で印が付く |

- 優先順の理由: 紙で練習できないもの(③⑥)を先に作る。
- 打ち切り条件: 開発3日目の終わりの時点で残っているフェーズは作らない。Phase4が未完成でも、Phase0の簡易履歴の表で成績は見られる。
- Phase0が1セッションで実機表示までいかなければ、構成(ES Modulesの読み込みパス、Pagesの設定)を先に疑う。

## 10. CLAUDE.md テンプレート(リポジトリ直下に置く)
```
# CLAUDE.md
- 仕様は SPEC.md が唯一の正。仕様にないことを実装するときは、先に提案して承認を得る。
- 成績データ(apt_results)は append-only。既存要素の書き換え・削除をするコードを書かない。
- ロジック(js/logic/)は DOM に触れない純粋関数にし、node --test で単体テストする。テストは先に書き、わざと壊して落ちることも確認する。
- 依存パッケージ・ビルド工程・フレームワークを追加しない。
- 数値はコードに直接書かず、settings.js の既定値を経由する。
- 各フェーズの終わりに PHASE_NOTES.md(完了/未解決/仮定/次の初手)を更新し、git commit する。
- 「動くはず」で完了にしない。スクリーンショット・テスト出力・localStorageの中身を証拠として出す。
```

## 11. 初手
リポジトリを作り、SPEC.md と CLAUDE.md を置く。Phase0 の計画(ファイル構成・画面遷移・保存形式)を提示し、承認を待つ。
